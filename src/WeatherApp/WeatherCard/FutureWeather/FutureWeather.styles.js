import styled from "styled-components";

export const FutureWeather__Container = styled.div`
    display: flex;
    flex-wrap: nowrap;
    overflow: hidden;
    justify-content: space-between;
    gap: 1.5rem;
    z-index: 1;
    position: relative; 
    
`